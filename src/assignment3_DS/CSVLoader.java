package assignment3_DS;


/**
 * Created by pv42 on 29.05.2017.
 * This Class is used to load CVS node/path files.
 */
public class CSVLoader {
    /** loades both the node and paths from CVS files and creates a graph
     * @param nodeFilePath indicates the absolute or relative path of the cvs file containing the nodes
     * @param pathFilePath indicates the absolute or relative path of the cvs file containing the nodes
     * @return the graph with nodes and paths loaded from files or {@code null} if there was a problem
     */
    public static Graph loadGraph(String nodeFilePath, String pathFilePath) {
        return null;
    }
}
